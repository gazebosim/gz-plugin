name: Bazel CI
on:
  push:
    branches: [gz-plugin3, main]
  pull_request:
    branches: [gz-plugin3, main]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
<<<<<<< HEAD
    uses: bazel-contrib/.github/.github/workflows/bazel.yaml@v7
=======
    uses: bazel-contrib/.github/.github/workflows/bazel.yaml@v7.7.0
>>>>>>> cb46527 ([bazel/infra] Update bazel CI to use workflow v7.7.0 (#215))
    with:
      folders: |
        [
          "."
        ]
      exclude: |
        [
          {"folder": ".", "bzlmodEnabled": false}
        ]
